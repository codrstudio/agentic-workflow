import type { AvatarUser } from "@ui/components/app-nav-panel/avatar-menu"

// TODO: substituir por dados reais do auth quando implementado
export const mockUser: AvatarUser = {
  name: "Usuario",
  role: "Administrador",
  email: "usuario@exemplo.com",
}
